<p id="notice"><%= notice %></p>

<!--<table>-->
  <!--<thead>
    <tr>
      <th>Name</th>
      <th>Person</th>
      <th>Description</th>
      <th>Difficulty</th>
      <th>Picture</th>
      <th colspan="3"></th>
    </tr>
  </thead>-->


  <div class="centreToi">
    <% @tricks.each do |trick| %>
      <tr>
        <td>
          <%= link_to trick do%>
            <img src="<%= trick.picture %>"/>
            <!--<p>voir plus</p>-->
          <% end %>
        </td>
        <!--<td><%= link_to 'Edit', edit_trick_path(trick) %></td>
        <td><%= link_to 'Destroy', trick, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
      </tr>
    <% end %>
  </div>


<br>

<%= link_to 'New Trick', new_trick_path %>
